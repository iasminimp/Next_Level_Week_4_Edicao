import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
    <App />,
  document.getElementById('root')
);

//App -> componente do react (exemplo: cabeçalho, menu, etc) => o que se pode repetir